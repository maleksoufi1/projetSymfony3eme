
{% extends 'base-back.html.twig' %}


{% block body %}
<head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text></svg>">
        {# Run `composer require symfony/webpack-encore-bundle` to start using Symfony UX #}
        <!-- CSS only -->
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
        {% block stylesheets %}
            {{ encore_entry_link_tags('app') }}
        {% endblock %}

        {% block javascripts %}
            {{ encore_entry_script_tags('app') }}
        {% endblock %}
    </head>
    <div class="container">
            <div class="row">
    <h3 class="mt-5 mb-5">Ajouter suivi programme</h3>
    {% for message in app.flashes('notice') %}
    <div class="alert alert-success">
    {{message}}
    </div>
    {% endfor %}
            <div class="col-md-6 offset-md-3">

    {{ form_start(formUpdateSuiviProg) }}
                

               
                <div class="form_group">
                    {{ form_label(formUpdateSuiviProg.note) }}
                    {{ form_widget(formUpdateSuiviProg.note,{'attr':{'class':'form_control' }})}}
                </div>

               
                <div class="form_group">
                    {{ form_label(formUpdateSuiviProg.remarque) }}
                    {{ form_widget(formUpdateSuiviProg.remarque,{'attr':{'class':'form_control' }})}}
                </div>

                <div class="form_group">
                    {{ form_label(formUpdateSuiviProg.programme) }}
                    {{ form_widget(formUpdateSuiviProg.programme,{'attr':{'class':'form_control' }})}}
                </div>

                <div class="form_group">
                    {{ form_label(formUpdateSuiviProg.user) }}
                    {{ form_widget(formUpdateSuiviProg.user,{'attr':{'class':'form_control' }})}}
                </div>

                




                

                
                <div class="form_group">
                    <button class ="btn btn-info mt-2 text-white">Submit</button>
                </div>


                 
                
                
              
        

    {{ form_end(formUpdateSuiviProg) }}
            </div>
            </div>
    </div>

{% endblock %}