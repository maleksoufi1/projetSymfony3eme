{% extends 'base-back.html.twig' %}



{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>


<table class="table">
<thead> 
<tr>
<th> Id</th>
<th> Catégorie événement </th>
<th> Prix</th>
<th> Description</th>
<th> Horraire</th>
<th> Quantite</th>
</tr>
</thead>
<tr {% for b in billets%}>
<td>{{b.id}}</td>
<td>{{b.evenement}}</td>
<td>{{b.numero}}</td>
<td>{{b.detail}}</td>
<td>{{b.horaire | date("m/d/Y")}}</td>
<td>{{b.quantite}}</td>
<td><a href="{{ path( 'deleteBillet' , {'id':b.id} )}}"> Delete </a> </td>
<td><a href="{{ path( 'updateBillet' , {'id':b.id} )}}"> Update </a> </td>
</tr {% endfor %}>
<td><a href="{{ path( 'addBillet'  )}}"> Create New </a> </td>
</table>

{% endblock %}